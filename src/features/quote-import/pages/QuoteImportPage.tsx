import { useReducer, useState, type ChangeEvent } from 'react';
import { CheckCircle2, FileText, UploadCloud } from 'lucide-react';
import { EmptyState, PageHeader } from '../../../components/ui';
import { validateCsvSourceFile, sourceFileToText } from '../parsers/csv';
import { parseCsv } from '../parsers/csv';
import { suggestColumnMappings, validateMappings } from '../mapping/mapping';
import { approveQuoteImportLocally, buildNormalizedQuote } from '../domain/drafts';
import { validateNormalizedQuote } from '../validation/validate';
import { computeFileSha256, maskFileHash } from '../domain/hash';
import { initialQuoteImportState, quoteImportReducer } from '../state/reducer';
import type { QuoteSourceFile } from '../domain/types';

const steps = ['Sélection du fichier','Contrôle du fichier','Mapping','Prévisualisation','Erreurs et avertissements','Résumé','Approbation locale'];
const statusStep: Record<string, number> = { FILE_SELECTED: 1, HASHING: 2, PARSING: 2, MAPPING_REQUIRED: 3, READY_FOR_REVIEW: 4, VALIDATION_ERROR: 5, APPROVED_LOCALLY: 7, SERVER_CONFIRMATION_REQUIRED: 7, REJECTED: 1 };
export function QuoteImportPage() {
  const [state, dispatch] = useReducer(quoteImportReducer, initialQuoteImportState); const [dragActive, setDragActive] = useState(false); const step = statusStep[state.status] ?? 1;
  const processFile = async (file: File) => { const source: QuoteSourceFile = { name: file.name, size: file.size, type: file.type, lastModified: file.lastModified, bytes: new Uint8Array(await file.arrayBuffer()) }; dispatch({ type: 'SELECT_FILE', file: source }); const fileCheck = validateCsvSourceFile(source); if (!fileCheck.valid) { dispatch({ type: 'PARSING_FAILURE', issues: fileCheck.issues }); return; } dispatch({ type: 'START_HASHING' }); const hash = await computeFileSha256(source.bytes); dispatch({ type: 'HASH_SUCCESS', hash: { algorithm: 'SHA-256', value: hash } }); const parsed = parseCsv(sourceFileToText(source)); if (!parsed.sheet || parsed.issues.some((issue) => issue.severity === 'BLOCKING')) { dispatch({ type: 'PARSING_FAILURE', issues: parsed.issues }); return; } dispatch({ type: 'PARSING_SUCCESS', sheet: parsed.sheet, mappings: suggestColumnMappings(parsed.sheet) }); };
  const onFileChange = (event: ChangeEvent<HTMLInputElement>) => { const file = event.target.files?.[0]; if (file) void processFile(file); };
  const updateMapping = (target: string, sourceColumn: string) => dispatch({ type: 'UPDATE_MAPPING', mappings: state.mappings.map((mapping) => mapping.target === target ? { ...mapping, sourceColumn: sourceColumn || undefined, confidence: sourceColumn ? 1 : 0, ambiguous: false, reason: 'Correction manuelle' } : mapping) });
  const approveMapping = () => { if (!state.sheet) return; const checked = validateMappings(state.mappings, state.sheet); if (!checked.valid) { dispatch({ type: 'VALIDATION_FAILURE', issues: checked.errors.map((message) => ({ code: 'REQUIRED_FIELD_MISSING', severity: 'BLOCKING', message })) }); return; } dispatch({ type: 'APPROVE_MAPPING' }); const quote = buildNormalizedQuote(state.sheet, state.mappings); const validation = validateNormalizedQuote(quote); dispatch({ type: 'RUN_VALIDATION', quote, summary: validation.summary, issues: validation.issues }); };
  const approveLocally = () => { if (!state.quote || !state.file || !state.hash) return; const approval = approveQuoteImportLocally(state.quote, state.file.name, state.hash.value, crypto.randomUUID(), state.issues); if (approval.status === 'SERVER_CONFIRMATION_REQUIRED') dispatch({ type: 'APPROVE_LOCALLY', approval }); else dispatch({ type: 'VALIDATION_FAILURE', issues: approval.issues }); };
  return <><PageHeader eyebrow="Import contrôlé" title="Importer un devis" description="CSV UTF-8 uniquement. L’approbation produit un brouillon en mémoire et exige encore une confirmation serveur."/><div className="mb-6 grid grid-cols-2 gap-2 md:grid-cols-7">{steps.map((label, index) => <div key={label} className={`rounded-lg border p-3 text-center text-xs ${index + 1 === step ? 'border-teal bg-teal/10 text-ink' : 'border-line bg-white text-slate-500'}`}><span className="font-bold">{index + 1}</span><span className="mt-1 hidden md:block">{label}</span></div>)}</div><section className="card"><div className="flex items-start justify-between gap-4"><div><h2 className="section-title">{steps[step - 1]}</h2><p className="mt-1 text-sm text-slate-500">Formats futurs documentés : XLSX, PDF structuré et export ERP. Ils ne sont pas supportés dans cette phase.</p></div><span className="rounded-full bg-mist px-3 py-1 text-xs font-semibold text-teal">{state.status}</span></div>{!state.file && <label onDragOver={(event) => { event.preventDefault(); setDragActive(true); }} onDragLeave={() => setDragActive(false)} onDrop={(event) => { event.preventDefault(); setDragActive(false); const file = event.dataTransfer.files[0]; if (file) void processFile(file); }} className={`mt-6 block cursor-pointer rounded-xl border-2 border-dashed p-10 text-center ${dragActive ? 'border-teal bg-teal/5' : 'border-line bg-mist'}`}><UploadCloud className="mx-auto text-teal" size={34}/><span className="mt-3 block font-medium text-ink">Déposer un devis CSV UTF-8</span><span className="mt-2 block text-sm text-slate-500">ou sélectionner un fichier dans l’explorateur</span><input aria-label="Sélectionner un fichier CSV" className="sr-only" type="file" accept=".csv,text/csv" onChange={onFileChange}/></label>}{state.file && <div className="mt-6 grid gap-4 md:grid-cols-3"><div className="rounded-lg border border-line p-4"><FileText className="text-teal" size={20}/><p className="mt-2 font-medium text-ink">{state.file.name}</p><p className="text-xs text-slate-500">{state.file.size.toLocaleString('fr-FR')} octets</p></div><div className="rounded-lg border border-line p-4"><p className="text-xs uppercase tracking-wide text-slate-500">Hash SHA-256</p><p className="mt-2 font-mono text-sm text-ink">{state.hash ? maskFileHash(state.hash.value) : 'Calcul en cours…'}</p></div><div className="rounded-lg border border-line p-4"><p className="text-xs uppercase tracking-wide text-slate-500">Séparateur</p><p className="mt-2 font-medium text-ink">{state.sheet?.delimiter ?? 'Détection en cours…'}</p></div></div>}{state.sheet && <div className="mt-6"><h3 className="font-semibold text-ink">Mapping des colonnes</h3><div className="mt-3 grid gap-3 md:grid-cols-2">{state.mappings.filter((mapping) => ['line_label','quote_reference','quantity','total_price','quote_date'].includes(mapping.target)).map((mapping) => <label className="text-sm" key={mapping.target}><span className="mb-1 block text-slate-600">{mapping.target}</span><select className="min-h-11 w-full rounded-lg border border-line bg-white px-3" value={mapping.sourceColumn ?? ''} onChange={(event) => updateMapping(mapping.target, event.target.value)}><option value="">Ignorer</option>{state.sheet?.headers.map((header) => <option key={header} value={header}>{header}</option>)}</select><span className="text-xs text-slate-500">Confiance : {Math.round(mapping.confidence * 100)}%</span></label>)}</div><button className="btn-primary mt-5" onClick={approveMapping} disabled={!state.sheet}>Prévisualiser et valider le mapping</button></div>}{state.quote && <div className="mt-6 overflow-x-auto"><h3 className="font-semibold text-ink">Prévisualisation des lignes</h3><table className="mt-3 w-full min-w-[680px] text-left text-sm"><thead><tr className="border-b border-line text-xs uppercase text-slate-500"><th className="p-2">Ligne</th><th className="p-2">Libellé normalisé</th><th className="p-2">Catégorie</th><th className="p-2">Étape</th><th className="p-2">Montant</th></tr></thead><tbody>{state.quote.lines.map((line) => <tr className="border-b border-line" key={line.sourceRowNumber}><td className="p-2">{line.sourceRowNumber}</td><td className="p-2">{line.normalizedLabel || '—'}</td><td className="p-2">{line.operationCategory}</td><td className="p-2">{line.stage}</td><td className="p-2">{line.totalPrice.toFixed(2)} TND</td></tr>)}</tbody></table></div>}{state.summary && <div className="mt-6 grid gap-3 sm:grid-cols-3"><div className="rounded-lg bg-mist p-4"><p className="text-xs text-slate-500">Lignes valides</p><p className="mt-1 text-2xl font-semibold text-ink">{state.summary.validRows}</p></div><div className="rounded-lg bg-mist p-4"><p className="text-xs text-slate-500">Lignes rejetées</p><p className="mt-1 text-2xl font-semibold text-ink">{state.summary.rejectedRows}</p></div><div className="rounded-lg bg-mist p-4"><p className="text-xs text-slate-500">Montant total</p><p className="mt-1 text-2xl font-semibold text-ink">{state.summary.totalAmount.toFixed(2)} {state.summary.currency}</p></div></div>}{state.issues.length > 0 && <div className="mt-6 rounded-lg border border-amber-200 bg-amber-50 p-4"><h3 className="font-semibold text-amber-900">Erreurs et avertissements</h3><ul className="mt-2 space-y-1 text-sm text-amber-900">{state.issues.map((issue, index) => <li key={`${issue.code}-${index}`}>{issue.severity} — {issue.message}</li>)}</ul></div>}{state.approval && <div className="mt-6 flex items-start gap-3 rounded-lg border border-teal/30 bg-teal/10 p-4 text-ink"><CheckCircle2 className="shrink-0 text-teal"/><div><p className="font-semibold">Brouillon validé localement — confirmation serveur requise</p><p className="mt-1 text-sm text-slate-600">Aucun devis ni dossier n’a été enregistré en base.</p></div></div>}{state.quote && !state.approval && <div className="mt-6 flex flex-wrap gap-3"><button className="btn-secondary" onClick={() => dispatch({ type: 'RESET_IMPORT' })}>Annuler</button><button className="btn-primary" onClick={approveLocally} disabled={state.status === 'VALIDATION_ERROR' || !state.summary || state.summary.blockingIssues > 0}>Approuver localement</button></div>}{!state.file && <div className="mt-6"><EmptyState title="Aucun fichier sélectionné" message="Le brouillon sera perdu lors d’un rafraîchissement pendant cette phase."/></div>}</section></>;
}
