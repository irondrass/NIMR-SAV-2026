import type { NormalizedQuote, QuoteImportIssue, QuoteImportSummary } from '../domain/types';
export function validateNormalizedQuote(quote: NormalizedQuote): { valid: boolean; issues: QuoteImportIssue[]; summary: QuoteImportSummary } { const issues: QuoteImportIssue[] = []; if (!quote.lines.length) issues.push({ code: 'EMPTY_LINE_LABEL', severity: 'BLOCKING', message: 'Aucune ligne de travaux valide.' }); const validRows = quote.lines.filter((line) => line.validationStatus !== 'REJECTED' && line.normalizedLabel); for (const line of quote.lines) { if (!line.normalizedLabel) issues.push({ code: 'EMPTY_LINE_LABEL', severity: 'BLOCKING', message: 'Libellé de ligne vide.', sourceRowNumber: line.sourceRowNumber }); if (line.quantity <= 0) issues.push({ code: 'INVALID_NUMBER', severity: 'BLOCKING', message: 'La quantité doit être positive.', sourceRowNumber: line.sourceRowNumber }); } const summary: QuoteImportSummary = { totalRows: quote.lines.length, validRows: validRows.length, rejectedRows: quote.lines.length - validRows.length, blockingIssues: issues.filter((item) => item.severity === 'BLOCKING').length, warningCount: quote.lines.reduce((count, line) => count + line.normalizationWarnings.length, 0), totalAmount: quote.estimatedAmount, currency: quote.currency }; return { valid: summary.blockingIssues === 0 && validRows.length > 0, issues, summary }; }
